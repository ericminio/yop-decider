{
  "name": "yop-decider",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "prettier": "prettier --write .",
    "pretest": "npm run prettier",
    "test": "node --test-reporter spec --test $(find app domain -name '*.test.js')",
    "working": "touch ./events.json; docker-compose -f ./working/docker-compose.yml run --rm --service-ports working sh -c 'npm i ; sh'",
    "ci": "docker compose -f ./working/docker-compose.yml run --rm working sh -c 'npm i ; npm test'",
    "start": "node app/start.mjs"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "type": "module",
  "devDependencies": {
    "jsdom": "^27.2.0",
    "prettier": "^3.7.4"
  }
}
