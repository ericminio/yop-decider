{
  "name": "yop-decider",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "prettier": "prettier --write .",
    "pretest": "npm run prettier",
    "test": "node --test $(find . -name '*.test.js')",
    "working": "docker-compose -f ./working/docker-compose.yml run --rm working sh -c 'npm i ; sh'"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "type": "module",
  "dependencies": {
    "prettier": "^3.6.2"
  }
}
